#include<stdio.h>
#include<math.h>

int main() {
    int num;
    printf("Enter a number: ");
    scanf("%d", &num);
    for(int i =1,i<=n;i++)
    {
        if(i%5==0 && i%7==0)
        continue;;
        printf("%d", i);
    }
    return 0;
}